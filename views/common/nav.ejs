<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">StudyMatch</a>
    <% if (isLoggedIn) { %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
    <% } %>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <%if (isLoggedIn) { %>
        <ul class="navbar-nav mr-auto">
          <%if (userType === 'ADMIN') { %>
            <li class="nav-item active">
              <a class="nav-link" href="/admin">Administration</a>
            </li>
          <% } else { %>
            <li class="nav-item active">
                <a class="nav-link" href="/schedule">Schedule</a>
            </li>
            <%if (userType === 'STUDENT') { %>
              <li class="nav-item active">
                  <a class="nav-link" href="/chat">Chat</a>
              </li>
              <li class="nav-item active">
                <a class="nav-link" href="/profileSearch">Search Profiles</a>
              </li>
              <li class="nav-item active">
                  <a class="nav-link" href="/myprofile">My Profile</a>
              </li>
            <% } %>
          <% } %>
          <li class="nav-item active">
              <a class="nav-link" href="/account-management">Account Management</a>
          </li>
        </ul>

        <form class="form-inline my-2 my-lg-0" action="/logout" method="POST">
          <button class="btn btn-outline my-2 my-sm-0" type="submit" style="padding-left: 0; padding-right: 0;">Logout</button>
        </form>
      <% } %>
    </div>
  </nav>
